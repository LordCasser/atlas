[package]
name = "context"
version = "1.3.0"
edition = "2024"
description = "Atlas context builder — assembles code context views from graph + store"
license = "MIT"

[dependencies]
types = { path = "../types" }
db = { path = "../db" }
graph = { path = "../graph" }
anyhow = "1"

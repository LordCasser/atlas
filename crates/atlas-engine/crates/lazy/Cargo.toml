[package]
name = "lazy"
version = "1.5.1"
edition = "2024"
description = "Atlas lazy dataflow engine — planner, loader, and hardcoded budgets"
license = "MIT"

[dependencies]
types = { path = "../types" }
db = { path = "../db" }
extraction = { path = "../extraction" }
anyhow = "1"
blake3 = "1"
tracing = "0.1"

[features]
default = ["typescript", "arkts", "cangjie"]

typescript = ["extraction/typescript"]
arkts = ["extraction/arkts"]
cangjie = ["extraction/cangjie"]

[package]
name = "workspace"
version = "1.1.0"
edition = "2024"
description = "Atlas workspace abstraction — project root, well-known paths, and path resolution"
license = "MIT"

[dependencies]
anyhow = "1"

[dev-dependencies]
tempfile = "3"

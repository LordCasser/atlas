[package]
name = "domain-rules"
version.workspace = true
edition.workspace = true
description = "Atlas language-agnostic domain rules engine"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
db = { path = "../db" }
regex = "1"
anyhow = "1"

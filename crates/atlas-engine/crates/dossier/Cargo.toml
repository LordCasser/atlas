[package]
name = "dossier"
version.workspace = true
edition.workspace = true
description = "Atlas dossier builder — Symbol Dossier for atlas_explore"
license = "MIT"

[dependencies]
types = { path = "../types" }
db = { path = "../db" }
graph = { path = "../graph" }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
